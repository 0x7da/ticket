module.exports = {
    name: 'help',

    execute(client, message) {
        message.channel.send(`So konfigurierst Du die Ticketmeldung **setup** und um die Bot-Latenz zu erhalten **ping** ✅`);
    },
};